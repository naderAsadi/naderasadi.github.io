---
layout: post
title: On Positional Encoding and Context Limit of LLMs
date: 2023-10-15 15:09:00
description: On Positional Encoding and Context Limit of LLMs
tags: llms machine-learning
categories: ml-concepts
featured: true
---

# What is Context-Length?

**Context length** is the number of *tokens* a language model can process at once. It is the maximum length of the input sequence. It’s like the memory or attention span of the model. It is a predefined number in a transformer-based model such as ChatGPT and Llama.

A **Token** is the model’s way of representing a word with a series of numbers. 100 words is [about](https://help.openai.com/en/articles/4936856-what-are-tokens-and-how-to-count-them) 130 tokens. If a model does not recognize a word, it breaks the word into multiple tokens.

Context length is important because

- **Complexity of input**. It limits the number of words a language model can process.
    - A model can only summarize an article no longer than the context length.
    - Long-term planning tasks require long input sequences.
    - Longer, more complex input generates richer output content.
- **Memory and coherence**. In chat apps, the context length dictates how much of the previous conversation is remembered. Language models like ChatGPT or [Llama 2-Chat](https://agi-sphere.com/llama-2/) do not remember anything. They are stateless. They know the past conversation because they were included in your current input before feeding it to the model.

Examples of context lengths for several well-known models.